apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: webcommerce-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.size | default "5Gi" }}
